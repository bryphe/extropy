/// <reference path="../_references.ts" />

module Extropy {

    export interface IViewportManager {
        width: number;
        height: number;

        element: HTMLElement;
        unscaledElement: HTMLElement;
    }
}

